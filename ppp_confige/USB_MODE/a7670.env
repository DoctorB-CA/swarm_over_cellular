# A7670E USB setup config
# Set your APN here:
APN=net.hotm

# USB_MODE can be: ecm | rndis | auto
USB_MODE=ecm

# Prefer public IP via USBNET if your carrier supports it (0=private, 1=public)
USBNETIP=0

# Optional: force radio full functionality (1) each boot
FORCE_CFUN=1
